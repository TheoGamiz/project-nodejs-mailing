doctype html 
html
    head
        title=data.id
       
    body 
        h1=data.name 
        p=data.mail
        br
        a(href=`/update/${data.id}`) Update contact.
        br
        a(href=`/delete/${data.id}`) Delete contact.
        br
        a(href="/") Menu principal